# Scripts

Use this folder for small operational commands.

Examples:

- Data download helpers
- Model training commands
- Report generation commands
- Local cleanup commands

Keep app logic inside `src/`; scripts should call application modules rather than duplicate them.
