services:
  safecity-dashboard:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "8501:8501"
    env_file:
      - ../.env
    command: streamlit run frontend/app.py --server.address=0.0.0.0
