# Maps

This folder is reserved for geospatial outputs and reusable map assets.

- `heatmaps/` stores generated hotspot visuals.
- `geojson/` stores boundaries and geospatial exchange files.
- `layers/` stores map layer definitions or exports.
